# Set this to your deployed Render backend URL in Vercel's environment variables
# Leave empty when running locally (Vite proxy handles /api → localhost:5000)
VITE_API_URL=https://your-render-app.onrender.com/api
